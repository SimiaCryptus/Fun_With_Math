{
  "type": "module",
  "name": "mathematical-explorations",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.web.json",
    "typecheck:watch": "tsc -p tsconfig.json --watch",
    "manifest:split": "tsx scripts/split-manifest.ts",
    "manifest:split:dry": "tsx scripts/split-manifest.ts --dry-run",
    "manifest:split:check": "tsx scripts/split-manifest.ts --dry-run --strict",
    "manifest:build": "tsx scripts/build-manifest.ts",
    "split:list": "node split_repo.js --list",
    "split:dry": "node split_repo.js --dry-run",
    "split": "node split_repo.js",
    "test": "node test/run-all.js",
    "serve": "npx http-server -p 8080 -c-1",
    "sitemap": "node scripts/generate-sitemap.js",
    "og-image": "node scripts/generate-og-image.js",
    "assets": "npm run sitemap && npm run og-image",
    "lint": "eslint '**/*.js'",
    "lint:fix": "eslint --fix '**/*.js'",
    "lint:css": "stylelint '**/*.css'",
    "lint:css:fix": "stylelint --fix '**/*.css'",
    "lint:html": "htmlhint '**/*.html'",
    "lint:md": "markdownlint-cli2 '**/*.md'",
    "lint:md:fix": "markdownlint-cli2 --fix '**/*.md'",
    "lint:all": "npm run lint && npm run lint:css && npm run lint:html && npm run lint:md",
    "lint:all:fix": "npm run lint:fix && npm run lint:css:fix && npm run lint:md:fix",
    "format": "prettier --write '**/*.js' '**/*.html' '**/*.css' '**/*.json' '**/*.md'",
    "format:check": "prettier --check '**/*.js' '**/*.html' '**/*.css' '**/*.json' '**/*.md'",
    "validate": "npm run typecheck && npm run lint:all && npm run format:check && npm run test",
    "prebuild": "npm run format",
    "build": "npm run assets",
    "build:seo": "node scripts/generate-seo-pages.cjs",
    "build:seo:prod": "node scripts/generate-seo-pages.cjs --out companion --base https://math.cognotik.com"
  },
  "author": "SimiaCryptus",
  "dependencies": {
    "canvas": "^3.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.8.2",
    "tsx": "^4.19.2",
    "@bubblewrap/cli": "^1.24.1",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "htmlhint": "^1.1.4",
    "markdownlint-cli2": "^0.13.0",
    "prettier": "^3.0.0",
    "stylelint": "^16.6.1",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-prettier": "^5.0.0",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}